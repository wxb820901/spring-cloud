version: '3'
services:
  localstack:
    image: localstack/localstack
    ports:
      - "4567-4584:4567-4584"
#      - "${PORT_WEB_UI-8080}:${PORT_WEB_UI-8080}"
    environment:
      - "SERVICES=s3,sqs,lambda,cloudwatch,sns,iam"
      - "DEBUG=1"
      - "DATA_DIR=C:/Users/bill_wang/localstack"
      - "LAMBDA_EXECUTOR=local"
      - "DOCKER_HOST=unix:///var/run/docker.sock"
      - "LOCALSTACK_HOSTNAME=localstack"
    volumes:
      - "C:/Users/bill_wang/localstack/ls_tmp:/tmp/localstack"
      - "C:/Users/bill_wang/localstack/var/run/docker.sock:/var/run/docker.sock"

